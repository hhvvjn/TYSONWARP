<div align="center">

![TYSONWARP Banner](assets/banner.svg)

# ⛏ TYSONWARP — AmneziaWG Config Forge

**Forge WARP-compatible [AmneziaWG](https://docs.amnezia.org/documentation/amnezia-wg/) configs instantly — fully offline, installable PWA.**

[![Version](https://img.shields.io/badge/version-0.6.8-a78bfa?style=for-the-badge)]()
[![PWA Ready](https://img.shields.io/badge/PWA-ready-67e8f9?style=for-the-badge&logo=pwa)]()
[![License](https://img.shields.io/badge/license-MIT-fcd34d?style=for-the-badge)](LICENSE)
[![Languages](https://img.shields.io/badge/langs-FA%20%7C%20EN%20%7C%20RU-6ee7b7?style=for-the-badge)](#)
[![Offline](https://img.shields.io/badge/offline-first-67e8f9?style=for-the-badge)](#)



</div>

---

## 📑 Table of Contents / فهرست / Содержание

- [English](#-english)
- [فارسی](#-فارسی)
- [Русский](#-русский)
- [License](#-license--مجوز--лицензия)
- [Credits](#-credits)

---

# 🇬🇧 English

## ✨ What is TYSONWARP?

**TYSONWARP** is a single-page, offline-first **Progressive Web App (PWA)** that generates
[AmneziaWG](https://amnezia.org/) configs compatible with **Cloudflare WARP**. It is engineered
around a battle-tested "golden formula" so the generated configs connect **instantly** in the
Amnezia app — no endless "Connecting…" spinner.

> ⚠️ **Important:** TYSONWARP **only** outputs the **AmneziaWG** format. It never produces plain
> WireGuard or any other protocol, because AmneziaWG's obfuscation layer is what makes WARP
> traffic resilient under DPI.

## 🌟 Key Features

| Feature | Description |
|---|---|
| 🛡 **Golden Formula** | `S1=0, S2=0`, `H1=1..H4=4`, `Jc=8, Jmin=55, Jmax=140` — tuned for instant handshake |
| 🔢 **Protocol Versions** | Supports AmneziaWG **v1.5**, **v2.0** and **v3.1** (adds `S3`/`S4`, magic packets `I1–I5`) |
| 📦 **IP Vault** | 18 tools: add, search, filter-by-port, sort, shuffle, dedupe, star, import/export, 3 save slots |
| ⚡ **Auto Select** | One-tap picks a random endpoint from the vault straight into the target field |
| 💎 **Bulk Forge** | Generate up to **10 configs at once**, each with its own QR code |
| 🖼 **Offline QR Engine** | A full QR generator written in pure JS — **zero CDN dependency**, works everywhere |
| 🔐 **Auto Key Generation** | The private key is created automatically before forging — you never hit a "missing key" error |
| 🌀 **Warp-in-Warp** | Double-layer tunneling with duplicate-endpoint protection |
| 🌍 **Trilingual UI** | فارسی / English / Русский with full RTL support |
| 📴 **Offline PWA** | Service Worker caches the app shell — runs with **no internet** after first load |
| 🎨 **4 Soft Themes** | Violet, Ocean, Sunset, Forest — easy on the eyes |

## 🚀 Live Demo

Open **** in any modern browser (Chrome, Edge, Safari, Firefox).

## 📲 Install as a PWA

### 🪟 Windows (Chrome / Edge)
1. Open the site in **Edge** or **Chrome**.
2. Click the **install icon** ⊕ in the address bar (or `Menu → Apps → Install TYSONWARP`).
3. TYSONWARP appears in your **Start Menu** and runs in its own window.

### 🍎 iOS (Safari)
1. Open the site in **Safari**.
2. Tap the **Share** button (square with arrow).
3. Scroll and tap **Add to Home Screen**.
4. TYSONWARP launches full-screen like a native app.

### 🤖 Android (Chrome)
1. Open the site in **Chrome**.
2. Tap `⋮` menu → **Install app** (or **Add to Home screen**).

> The PWA uses `assets/icon-maskable.png` for adaptive icons on Windows/Android and
> `assets/apple-touch-icon.png` for the iOS home-screen icon.

## 📖 Usage Tutorial

1. **Open TYSONWARP.** The IP Vault loads with 87 pre-seeded Cloudflare endpoints.
2. **Choose a target endpoint.** Either click the ⚡ **Auto Select** button, press 🎲 random, or use
   the 🎯 target icon on any row in the vault.
3. **Pick a protocol version** — `v1.5`, `v2.0` or `v3.1`.
4. **Pick an output mode:**
   - 🛡 **Anti-filter** (default): adds obfuscation params for DPI-heavy networks.
   - ⚡ **FAST (WARP)**: minimal params for maximum speed.
5. **Press ⚒ Forge.** TYSONWARP auto-generates the private key if needed, builds the config, runs a
   sanity check, and shows the result with a **SANITY: PASS** badge.
6. **Copy, download, or scan.** Use 📋 Copy, 💾 `.CONF`, or 🖼 **QR Code**.
   Scan the QR directly from the Amnezia app's import screen.
7. **Bulk Forge** (💎) builds up to 10 configs at once from your selected endpoints — each with QR.

## 🧠 The Golden Formula Explained

```
[Interface]
PrivateKey = <auto-generated>
Address    = 172.16.0.2/32, 2606:4700:110:.../128
DNS        = 8.8.8.8
MTU        = 1280
Jc = 8            ← junk packet count
Jmin = 55         ← min junk size
Jmax = 140        ← max junk size
S1 = 0            ← init padding
S2 = 0            ← response padding
H1 = 1            ← init header
H2 = 2            ← response header
H3 = 3            ← underload header
H4 = 4            ← transport header

[Peer]
PublicKey = bmXOC+F1FxEMF9dyiK2H5/1SUtzH0JuVo51h2wPfgyo=
AllowedIPs = 0.0.0.0/0, ::/0
Endpoint   = <chosen endpoint>
PersistentKeepalive = 25
```

This exact combination is what avoids the dreaded **"Connecting…" stuck state** in the Amnezia app.

## 🗂 Project Structure

```
TYSONWARP/
├── index.html               ← the entire app (self-contained)
├── manifest.webmanifest     ← PWA manifest
├── sw.js                    ← Service Worker (offline cache)
├── robots.txt
├── .nojekyll                ← empty file, disables Jekyll on GitHub Pages
├── README.md
├── assets/
│   ├── logo.svg             ← source logo
│   ├── banner.svg           ← README banner
│   ├── icon-192.png         ← PWA icon 192
│   ├── icon-512.png         ← PWA icon 512
│   ├── icon-maskable.png    ← adaptive/maskable icon
│   └── apple-touch-icon.png ← iOS home-screen icon
└── tools/
    └── icon-generator.html  ← generates all PNG icons in-browser
```

## 🛠 Development & Setup

```bash
# Clone the repository
git clone 
cd TYSONWARP

# (one-time) Generate the PNG icons:
#   open tools/icon-generator.html in a browser and click "Download All",
#   then move the files into assets/

# Serve locally (any static server works)
python -m http.server 8080
# → http://localhost:8080
```

### Deploy to GitHub Pages
1. Push the repo to GitHub.
2. Go to **Settings → Pages → Source: main branch / root**.
3. Ensure an empty `.nojekyll` file exists at the repo root.
4. Your site goes live at `https://<username>.github.io/<repo>/`.

## 🔒 Privacy

- **No servers, no tracking.** Everything runs in your browser.
- Keys are generated **locally** with the Web Crypto API.
- All data (vault, settings, keys) stays in your browser's `localStorage`.

---

# 🇮🇷 فارسی

## ✨ وارپ‌ماینر چیست؟

**وارپ‌ماینر** یک **وب‌اپ پیش‌رو (PWA)** تک‌صفحه‌ای و آفلاین است که کانفیگ‌های
[AmneziaWG](https://amnezia.org/) سازگار با **کلادفلر وارپ** می‌سازد. هستهٔ برنامه بر پایهٔ
«فرمول طلایی» آزمایش‌پس‌داده ساخته شده تا کانفیگ‌ها **فوراً** در اپلیکیشن Amnezia متصل شوند —
بدون گیر کردن روی «در حال اتصال…».

> ⚠️ **نکته:** وارپ‌ماینر **فقط** خروجی **AmneziaWG** تولید می‌کند و هرگز WireGuard خالص یا
> پروتکل دیگری نمی‌سازد؛ چون لایهٔ استتار AmneziaWG همان چیزی است که ترافیک وارپ را در برابر
> فیلترینگ مقاوم می‌کند.

## 🌟 قابلیت‌های کلیدی

| قابلیت | توضیح |
|---|---|
| 🛡 **فرمول طلایی** | `S1=0, S2=0` و `H1=1..H4=4` و `Jc=8, Jmin=55, Jmax=140` برای اتصال فوری |
| 🔢 **نسخه‌های پروتکل** | پشتیبانی از **v1.5**، **v2.0** و **v3.1** (با `S3`/`S4` و پکت‌های جادویی `I1–I5`) |
| 📦 **مخزن آی‌پی** | ۱۸ ابزار: افزودن، جستجو، فیلتر پورت، مرتب‌سازی، بر زدن، حذف تکراری، ستاره، ایمپورت/اکسپورت، ۳ اسلات |
| ⚡ **انتخاب خودکار** | با یک کلیک، یک اندپوینت تصادفی از مخزن وارد فیلد هدف می‌شود |
| 💎 **فورج انبوه** | ساخت تا **۱۰ کانفیگ هم‌زمان**، هرکدام با QR اختصاصی |
| 🖼 **موتور QR آفلاین** | تولیدکنندهٔ QR کاملاً جاوااسکریپتی — **بدون هیچ CDN**، همه‌جا کار می‌کند |
| 🔐 **تولید خودکار کلید** | کلید خصوصی قبل از ساخت به‌صورت خودکار ساخته می‌شود — هرگز خطای «کلید نداریم» نمی‌گیرید |
| 🌀 **وارپ در وارپ** | تونل دولایه با محافظت در برابر اندپوینت تکراری |
| 🌍 **رابط سه‌زبانه** | فارسی / انگلیسی / روسی با پشتیبانی کامل راست‌به‌چپ |
| 📴 **آفلاین کامل** | سرویس‌ورکر پوستهٔ اپ را کش می‌کند — پس از بار اول **بدون اینترنت** کار می‌کند |
| 🎨 **۴ تم ملایم** | یاسی، اقیانوس، غروب، جنگل |

## 🚀 دموی زنده

نشانی **** را در هر مرورگر مدرن باز کنید.

## 📲 نصب به‌عنوان اپ

### 🪟 ویندوز (کروم / اج)
1. سایت را در **Edge** یا **Chrome** باز کنید.
2. روی آیکون **نصب** ⊕ در نوار آدرس بزنید (یا `منو → Apps → Install TYSONWARP`).
3. برنامه در **استارت منو** ظاهر می‌شود و در پنجرهٔ مستقل اجرا می‌شود.

### 🍎 آیفون (سافاری)
1. سایت را در **Safari** باز کنید.
2. دکمهٔ **اشتراک‌گذاری** (مربع با فلش) را بزنید.
3. گزینهٔ **Add to Home Screen** را انتخاب کنید.
4. وارپ‌ماینر مثل یک اپ واقعی تمام‌صفحه اجرا می‌شود.

### 🤖 اندروید (کروم)
1. سایت را در **Chrome** باز کنید.
2. منوی `⋮` → **Install app** (یا **Add to Home screen**).

## 📖 آموزش استفاده

1. **برنامه را باز کنید.** مخزن آی‌پی با ۸۷ اندپوینت پیش‌فرض کلادفلر بارگذاری می‌شود.
2. **اندپوینت هدف را انتخاب کنید.** دکمهٔ ⚡ **انتخاب خودکار**، 🎲 تصادفی، یا آیکون 🎯 در هر ردیف.
3. **نسخهٔ پروتکل** را برگزینید — `v1.5`، `v2.0` یا `v3.1`.
4. **حالت خروجی** را انتخاب کنید:
   - 🛡 **ضد فیلتر** (پیش‌فرض): پارامترهای استتار برای شبکه‌های فیلترشده.
   - ⚡ **سریع (WARP)**: کمترین پارامتر برای بیشترین سرعت.
5. **دکمهٔ ⚒ ساخت** را بزنید. کلید خصوصی خودکار ساخته می‌شود، کانفیگ ساخته و اعتبارسنجی می‌شود
   و نشان **SANITY: PASS** نمایش داده می‌شود.
6. **کپی، دانلود یا اسکن.** با 📋 کپی، 💾 فایل `.CONF` یا 🖼 **QR کد**.
   QR را مستقیم از صفحهٔ ایمپورت اپلیکیشن Amnezia اسکن کنید.
7. **فورج انبوه** (💎) تا ۱۰ کانفیگ یک‌جا از اندپوینت‌های انتخابی می‌سازد — هرکدام با QR.

## 🗂 ساختار پروژه

```
TYSONWARP/
├── index.html               ← کل اپلیکیشن (خودکفا)
├── manifest.webmanifest     ← مانیفست PWA
├── sw.js                    ← سرویس‌ورکر (کش آفلاین)
├── robots.txt
├── .nojekyll                ← فایل خالی برای غیرفعال‌سازی Jekyll
├── README.md
├── assets/                  ← لوگو، بنر و آیکون‌ها
└── tools/
    └── icon-generator.html  ← تولید آیکون‌های PNG در مرورگر
```

## 🛠 راه‌اندازی

```bash
git clone 
cd TYSONWARP
# برای تولید آیکون‌ها: tools/icon-generator.html را در مرورگر باز کنید
# و «Download All» را بزنید، سپس فایل‌ها را به assets/ منتقل کنید
python -m http.server 8080
```

### انتشار روی گیت‌هاب پیجز
1. ریپازیتوری را پوش کنید.
2. به **Settings → Pages → Source: main branch / root** بروید.
3. مطمئن شوید فایل خالی `.nojekyll` در ریشه وجود دارد.
4. سایت شما روی `https://<username>.github.io/<repo>/` منتشر می‌شود.

## 🔒 حریم خصوصی

- **بدون سرور، بدون ردیابی.** همه‌چیز در مرورگر شما اجرا می‌شود.
- کلیدها **محلی** با Web Crypto API تولید می‌شوند.
- همهٔ داده‌ها (مخزن، تنظیمات، کلیدها) در `localStorage` مرورگر شما می‌ماند.

---

# 🇷🇺 Русский

## ✨ Что такое TYSONWARP?

**TYSONWARP** — это одностраничное офлайн-**прогрессивное веб-приложение (PWA)**, которое создаёт
конфиги [AmneziaWG](https://amnezia.org/), совместимые с **Cloudflare WARP**. Ядро построено на
проверенной «золотой формуле», поэтому конфиги подключаются **мгновенно** — без бесконечного
«Подключение…».

> ⚠️ **Важно:** TYSONWARP создаёт **только** формат **AmneziaWG** и никогда не генерирует обычный
> WireGuard или другие протоколы.

## 🌟 Ключевые возможности

| Функция | Описание |
|---|---|
| 🛡 **Золотая формула** | `S1=0, S2=0`, `H1=1..H4=4`, `Jc=8, Jmin=55, Jmax=140` |
| 🔢 **Версии протокола** | **v1.5**, **v2.0** и **v3.1** (`S3`/`S4`, магические пакеты `I1–I5`) |
| 📦 **Хранилище IP** | 18 инструментов: добавить, поиск, фильтр по порту, сортировка, перемешать, убрать дубли, избранное, импорт/экспорт, 3 слота |
| ⚡ **Авто-выбор** | Одним нажатием случайный эндпоинт попадает в поле цели |
| 💎 **Массовая сборка** | До **10 конфигов сразу**, каждый со своим QR |
| 🖼 **Оффлайн QR-движок** | Полностью JS-генератор — **без CDN**, работает везде |
| 🔐 **Авто-ключ** | Приватный ключ создаётся автоматически перед сборкой |
| 🌀 **Warp-in-Warp** | Двухслойный туннель с защитой от дублей |
| 🌍 **Три языка** | فارسی / English / Русский с поддержкой RTL |
| 📴 **Оффлайн** | Service Worker кеширует приложение — работает **без интернета** |
| 🎨 **4 темы** | Фиолетовая, Океан, Закат, Лес |

## 🚀 Демо

Откройте **** в любом современном браузере.

## 📲 Установка как приложение

### 🪟 Windows (Chrome / Edge)
1. Откройте сайт в **Edge** или **Chrome**.
2. Нажмите значок **установки** ⊕ в адресной строке (или `Меню → Приложения → Установить`).
3. Приложение появится в **меню Пуск**.

### 🍎 iOS (Safari)
1. Откройте сайт в **Safari**.
2. Нажмите **«Поделиться»** (квадрат со стрелкой).
3. Выберите **«На экран Домой»**.

### 🤖 Android (Chrome)
1. Откройте сайт в **Chrome**.
2. Меню `⋮` → **Установить приложение**.

## 📖 Как пользоваться

1. **Откройте TYSONWARP.** Хранилище загружает 87 готовых эндпоинтов.
2. **Выберите целевой эндпоинт** — кнопка ⚡ «Авто», 🎲 случайный или 🎯 в списке.
3. **Выберите версию протокола** — `v1.5`, `v2.0` или `v3.1`.
4. **Выберите режим:** 🛡 Анти-фильтр (по умолчанию) или ⚡ FAST.
5. **Нажмите ⚒ Собрать.** Ключ создаётся автоматически, конфиг проверяется (значок **SANITY: PASS**).
6. **Копируйте, скачайте или отсканируйте QR** прямо в приложении Amnezia.
7. **Массовая сборка** (💎) создаёт до 10 конфигов сразу, каждый с QR.

## 🛠 Запуск

```bash
git clone 
cd TYSONWARP
# Для иконок: откройте tools/icon-generator.html и нажмите «Download All»
python -m http.server 8080
```

### Публикация на GitHub Pages
1. Запушьте репозиторий.
2. **Settings → Pages → Source: main branch / root**.
3. Убедитесь, что пустой файл `.nojekyll` есть в корне.

## 🔒 Приватность

- **Без серверов и слежки.** Всё работает в браузере.
- Ключи генерируются **локально** через Web Crypto API.
- Все данные хранятся в `localStorage` вашего браузера.

---

# 📄 License / مجوز / Лицензия

Released under the **MIT License** — see [LICENSE](LICENSE).

منتشرشده تحت **پروانهٔ MIT**.

Распространяется по **лицензии MIT**.

---

# 💛 Credits

<div align="center">



Inspired by the AmneziaVPN project and the WARP community.

If TYSONWARP helped you, please ⭐ **star** the repository!

</div>